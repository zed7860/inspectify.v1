import {S3Client,PutObjectCommand,GetObjectCommand} from '@aws-sdk/client-s3'; import {getSignedUrl} from '@aws-sdk/s3-request-presigner';
const s3=()=>new S3Client({region:process.env.STORAGE_REGION||'auto',endpoint:process.env.STORAGE_ENDPOINT,credentials:{accessKeyId:process.env.STORAGE_ACCESS_KEY_ID!,secretAccessKey:process.env.STORAGE_SECRET_ACCESS_KEY!},forcePathStyle:true}); export async function signedUpload(key:string,mime:string){return getSignedUrl(s3(),new PutObjectCommand({Bucket:process.env.STORAGE_BUCKET!,Key:key,ContentType:mime}),{expiresIn:600})} export async function signedRead(key:string){return getSignedUrl(s3(),new GetObjectCommand({Bucket:process.env.STORAGE_BUCKET!,Key:key}),{expiresIn:300})}

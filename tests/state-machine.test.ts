import {describe,it,expect} from "vitest";import {canTransition} from "@/lib/inspection/state-machine";describe("inspection workflow",()=>{it("prevents skipping PMC",()=>expect(canTransition("PENDING_PMC","FINAL_APPROVED")).toBe(false));it("allows PMC approval path",()=>expect(canTransition("PENDING_PMC","PENDING_CLIENT")).toBe(true));it("requires resubmission after rejection",()=>expect(canTransition("PMC_REJECTED","RESUBMITTED")).toBe(true));});

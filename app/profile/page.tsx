import {requireUser} from "@/lib/auth/guard";import {AppShell} from "@/components/app-shell";export default async function Page(){const u=await requireUser();const profile=u.profile;const companyName=(u as any).company?.name ?? null;return <AppShell user={profile}><div className="pagehead"><h1>Profile</h1></div><section className="card"><h2>{profile.name}</h2><p>{u.user.email}</p><p>{profile.role.replaceAll("_"," ")}{companyName?` · ${companyName}`:""}</p><p className="muted">Password resets are managed securely by an administrator.</p></section></AppShell>}
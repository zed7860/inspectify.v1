import {requireUser} from "@/lib/auth/guard";import {AppShell} from "@/components/app-shell";import {PasswordForm} from "@/components/password-form";export default async function Page(){const u=await requireUser();const profile=u.profile;const companyName=(u as any).company?.name ?? null;return <AppShell user={profile}><div className="pagehead"><h1>Profile</h1></div><section className="card"><h2>{profile.name}</h2><p>{u.user.email}</p><p>{profile.role.replaceAll("_"," ")}{companyName?` · ${companyName}`:""}</p></section><section className="card"><div className="section-heading"><div><p className="eyebrow">Account security</p><h2>Change password</h2></div></div><p className="muted">Use at least 12 characters. Your new password is updated securely by Supabase Auth.</p><PasswordForm /></section></AppShell>}
import {redirect} from "next/navigation";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;redirect(`/inspections${q.q?`?search=${encodeURIComponent(q.q)}`:""}`)}
import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";export async function POST(r:Request){const s=await createClient();const {error}=await s.auth.signOut();if(error)return NextResponse.json({error:"Unable to sign out. Please try again."},{status:400});return NextResponse.redirect(new URL("/login",r.url),303)}
